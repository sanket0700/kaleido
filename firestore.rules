rules_version = '2';

// Placeholder: deny everything until the real per-collection rules land
// (see the "Write Firestore + Storage security rules" task).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
