rules_version = '2';

// Placeholder: deny everything until the real per-path rules land
// (see the "Write Firestore + Storage security rules" task).
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
